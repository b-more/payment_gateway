# Shared TLS settings (§10.2). One SAN certificate covers all five hostnames
# (TLS-3); each HTTPS server includes this. Certs live in the letsencrypt volume;
# a self-signed bootstrap is generated on first boot so Nginx starts pre-Certbot.
ssl_certificate     /etc/letsencrypt/live/instacompayzm.com/fullchain.pem;
ssl_certificate_key /etc/letsencrypt/live/instacompayzm.com/privkey.pem;

ssl_protocols       TLSv1.2 TLSv1.3;
ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
ssl_prefer_server_ciphers off;
ssl_session_cache   shared:SSL:10m;
ssl_session_timeout 1d;
